server:
  port: 8080
spring:
  jpa:
    properties:
      hibernate:
        ddl-auto: create-drop
        generate-ddl: true
        show-sql: true
        database-platform: org.hibernate.dialect.H2Dialect
        open-in-view: false
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      username: sa
      password: blind3256@!
      jdbc-url: jdbc:h2:tcp://localhost/./db/eduwill
  h2:
    console:
      enabled: true
